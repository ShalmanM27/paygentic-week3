// LoanCard — three Locus session IDs side-by-side (target, repayment, customer revenue),
// each with status, amount, line items, payer/merchant wallets, txHash, expiration, raw JSON toggle.

export function LoanCard() {
  return null;
}
